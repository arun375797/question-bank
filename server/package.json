{
  "name": "domain-question-bank-server",
  "version": "1.0.0",
  "description": "Domain Question Bank API Server",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "seed": "node src/seed.js",
    "docker:build": "docker build -t question-bank-api .",
    "docker:run": "docker run -p 5000:5000 --env-file .env question-bank-api"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
