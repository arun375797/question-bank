{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rootDirectory": "client",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://16.16.217.71/api/:path*"
    }
  ]
}

